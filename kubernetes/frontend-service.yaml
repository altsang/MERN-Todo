apiVersion: v1
kind: Service
metadata:
  name: mern-todo-frontend-service
spec:
  selector:
    app: mern-todo-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
